# SharpCab

SharpCab is a lightweight .NET wrapper around the `cabextract` CLI tool.

It allows you to:
- List CAB archive entries
- Read files from CAB archives as streams

---

## ⚠️ Requirement

This library **depends on the external CLI tool**:

👉 `cabextract` must be installed and available on PATH.
